services:
  # -------------------------
  # Jenkins Agent (לפי Dockerfile.agent)
  # -------------------------
  jenkins-agent:
    build:
      context: .
      dockerfile: Dockerfile.agent
      # הוסף build arg כדי להכריח rebuild
      args:
        - BUILD_DATE=${BUILD_DATE:-$(date)}
    container_name: jenkins-agent
    user: root
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - jenkins-data:/var/jenkins_home
    networks:
      - jewelry-net
    restart: unless-stopped
    # Agent needs to stay running - use sleep for now
    command: ["sleep", "infinity"]
    depends_on:
      - jenkins

  # -------------------------
  # Nexus Repository
  # -------------------------
  nexus:
    image: sonatype/nexus3
    container_name: nexus
    ports:
      - "8081:8081"   # UI
      - "8082:8082"   # Docker registry
    volumes:
      - nexus-data:/nexus-data
    networks:
      - jewelry-net
    restart: unless-stopped

  # -------------------------
  # Jenkins Server
  # -------------------------
  jenkins:
    image: jenkins/jenkins:lts
    container_name: jenkins
    environment:
      - DOCKER_HOST=unix:///var/run/docker.sock
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - jenkins-data:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - jewelry-net
    restart: unless-stopped

# -------------------------
# Volumes
# -------------------------
volumes:
  jenkins-data:
  nexus-data:

# -------------------------
# Network
# -------------------------
networks:
  jewelry-net:
    driver: bridge